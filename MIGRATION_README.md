# Season2 Core ForgeGradle migration overlay

Copy the contents of this folder into the root of your existing Season2-Core repository.

This is **Phase 1** of the migration: it adds the real ForgeGradle/Java 17 build environment and CI without deleting or rewriting the current working 0.4.4 baseline.

Read `docs/FORGEGRADLE_STATUS.md` before committing.
