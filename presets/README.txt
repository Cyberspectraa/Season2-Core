Season 2 Core EasyNPC presets - 0.4.4-alpha.4

Spectral Post Courier
- Uses the bundled Season 2 postman skin: spectralmail:textures/entity/postman.png
- Import manually if desired, then use its operator-only bind dialogue.

Dragon Bank Banker
- Opens /dragonbank @initiator through dialogue.
- Dialogue accurately describes the current deposit-only bank.

EasyNPC 7.10.0 also supports third-party datapack presets. Copies are bundled under each mod namespace in data/<modid>/easy_npc/preset/.
